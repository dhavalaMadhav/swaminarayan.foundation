<!-- views/partials/header.ejs -->

<div class="side-indicator"></div>

<!-- Top Bar -->
<div class="top-bar">
    <div class="top-bar-content">
        <div class="social-links">
            <span>Follow Us:</span>
            <div class="social-icons">
                <a href="https://facebook.com" target="_blank"><i class="fab fa-facebook-f"></i></a>
                <a href="https://instagram.com" target="_blank"><i class="fab fa-instagram"></i></a>
                <a href="https://youtube.com" target="_blank"><i class="fab fa-youtube"></i></a>
                <a href="https://linkedin.com" target="_blank"><i class="fab fa-linkedin-in"></i></a>
            </div>
        </div>
        <div class="contact-info">
            <a href="/apply" style="text-decoration: none;">
                <button class="enroll-btn">ENROLL NOW</button>
            </a>
            <div class="contact-item">
                <i class="fas fa-envelope"></i>
                <span>info@swaminarayanuniversity.ac.in</span>
            </div>
            <div class="contact-item">
                <i class="fas fa-phone-alt"></i>
                <span>+91-63576 75561</span>
            </div>
        </div>
    </div>
</div>

<!-- Header -->
<header class="header">
    <div class="header-content">
        <!-- Logo Section -->
        <div class="logo-section">
            <a href="/" style="text-decoration: none;">
                <img src="/swaminarayan-logo.png" alt="Swaminarayan University" class="university-logo-full">
            </a>
        </div>

        <!-- Hamburger Menu Button (Mobile Only) -->
        <button class="mobile-menu-toggle" id="mobileMenuToggle">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <!-- Navigation Menu -->
        <nav class="nav-container" id="navContainer">
            <ul class="nav-menu">
                <li class="has-dropdown">
                    <a href="/about">ABOUT <i class="fas fa-chevron-down dropdown-icon"></i></a>
                    <div class="dropdown-menu">
                        <ul>
                            <li><a href="/about/about-su"><i class="fas fa-caret-right"></i> About SU</a></li>
                            <li><a href="/about/authorities"><i class="fas fa-caret-right"></i> Authorities Of University</a></li>
                            <li><a href="/about/recognitions"><i class="fas fa-caret-right"></i> Recognitions & Approvals</a></li>
                            <li><a href="/about/awards"><i class="fas fa-caret-right"></i> Awards & Achievements</a></li>
                        </ul>
                    </div>
                </li>
                <li><a href="/programs" class="highlight">PROGRAMS</a></li>
                <li class="has-dropdown">
                    <a href="/admissions" class="highlight">ADMISSIONS <i class="fas fa-chevron-down dropdown-icon"></i></a>
                    <div class="dropdown-menu">
                        <ul>
                            <li><a href="/admissions/how-to-apply"><i class="fas fa-caret-right"></i> How To Apply</a></li>
                            <li><a href="/admissions/online-application" class="highlight"><i class="fas fa-caret-right"></i> Online Application Form</a></li>
                            <li><a href="/admissions/scholarship"><i class="fas fa-caret-right"></i> Scholarship</a></li>
                        </ul>
                    </div>
                </li>
                <li><a href="/placement">PLACEMENT</a></li>
                <li><a href="/contact">CONTACT</a></li>
            </ul>
        </nav>
    </div>
</header>

<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Roboto', 'Helvetica Neue', 'Arial', sans-serif;
        background: #ffffff;
        overflow-x: hidden;
    }

    /* Top bar - MINIMAL SWISS */
    .top-bar {
        background: #ffffff;
        border-bottom: 1px solid #e0e0e0;
        padding: 8px 0;
        color: #1a1a1a;
        font-size: 14px;
    }

    .top-bar-content {
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 50px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: nowrap;
    }

    .social-links {
        display: flex;
        align-items: center;
        gap: 15px;
        flex-shrink: 0;
    }

    .social-links span {
        font-weight: 500;
        font-size: 14px;
        color: #666;
        letter-spacing: 0.5px;
    }

    .social-icons {
        display: flex;
        gap: 8px;
    }

    .social-icons a {
        width: 32px;
        height: 32px;
        background: transparent;
        border: 1px solid #e0e0e0;
        color: #1a1a1a;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        text-decoration: none;
        transition: all 0.3s ease;
        font-size: 14px;
    }

    .social-icons a:hover {
        background: #DC2626;
        border-color: #DC2626;
        color: white;
        transform: translateY(-2px);
    }

    .contact-info {
        display: flex;
        gap: 25px;
        align-items: center;
        flex-shrink: 0;
    }

    /* ENROLL BUTTON - SWAMINARAYAN COLORS */
    .enroll-btn {
        background: #DC2626;
        border: none;
        padding: 14px 32px;
        color: white;
        font-weight: 700;
        font-size: 15px;
        letter-spacing: 1.5px;
        cursor: pointer;
        transition: all 0.3s ease;
        font-family: 'Roboto', sans-serif;
        text-transform: uppercase;
        border-radius: 2px;
        box-shadow: 0 4px 15px rgba(220, 38, 38, 0.4);
    }

    .enroll-btn:hover {
        background: #B91C1C;
        box-shadow: 0 6px 20px rgba(220, 38, 38, 0.5);
        transform: translateY(-2px);
    }

    .contact-item {
        display: flex;
        align-items: center;
        gap: 6px;
    }

    .contact-item i {
        font-size: 14px;
        color: #FBBF24;
    }

    .contact-item span {
        font-size: 14px;
        font-weight: 400;
        color: #666;
    }

    /* Sticky header */
    .header {
        background-color: white;
        border-bottom: 1px solid rgba(0, 0, 0, 0.05);
        position: sticky;
        top: 0;
        z-index: 1000;
        transition: all 0.3s ease;
    }

    .header-content {
        max-width: 1400px;
        margin: 0 auto;
        padding: 15px 50px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: nowrap !important;
    }

    .logo-section {
        display: flex;
        align-items: center;
        z-index: 1001;
        flex-shrink: 0;
    }

    /* Logo Styling */
    .university-logo-full {
        height: 90px;
        width: auto;
        max-width: 100%;
        object-fit: contain;
        transition: all 0.3s ease;
    }

    /* Mobile Menu Toggle Button */
    .mobile-menu-toggle {
        display: none;
        flex-direction: column;
        justify-content: space-between;
        width: 30px;
        height: 24px;
        background: transparent;
        border: none;
        cursor: pointer;
        padding: 0;
        z-index: 1001;
        transition: all 0.3s ease;
    }

    .mobile-menu-toggle span {
        width: 100%;
        height: 3px;
        background: #1a1a1a;
        border-radius: 2px;
        transition: all 0.3s ease;
    }

    .mobile-menu-toggle.active span:nth-child(1) {
        transform: rotate(45deg) translate(8px, 8px);
        background: #DC2626;
    }

    .mobile-menu-toggle.active span:nth-child(2) {
        opacity: 0;
    }

    .mobile-menu-toggle.active span:nth-child(3) {
        transform: rotate(-45deg) translate(8px, -8px);
        background: #DC2626;
    }

    /* Navigation Container */
    .nav-container {
        display: flex;
        flex-shrink: 0;
    }

    .nav-menu {
        display: flex;
        list-style: none;
        gap: 5px;
        align-items: center;
        flex-wrap: nowrap;
    }

    .nav-menu li {
        position: relative;
    }

    .nav-menu a {
        text-decoration: none;
        color: #1a1a1a;
        padding: 8px 16px;
        display: flex;
        align-items: center;
        gap: 5px;
        font-size: 14px;
        font-weight: 500;
        transition: all 0.3s ease;
        white-space: nowrap;
        letter-spacing: 0.3px;
        text-transform: uppercase;
    }

    .nav-menu a:hover {
        color: #DC2626;
    }

    .nav-menu .dropdown-icon {
        font-size: 8px;
        margin-left: 2px;
    }

    .nav-menu .highlight {
        color: #DC2626;
    }

    /* Side indicator */
    .side-indicator {
        position: fixed;
        left: 0;
        top: 50%;
        transform: translateY(-50%);
        width: 3px;
        height: 80px;
        background: #DC2626;
        z-index: 999;
    }

    /* Dropdown Menu - SWISS STYLE */
    .has-dropdown {
        position: relative;
    }

    .dropdown-menu {
        position: absolute;
        top: 100%;
        left: 0;
        min-width: 260px;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(0, 0, 0, 0.08);
        opacity: 0;
        visibility: hidden;
        transition: all 0.3s ease;
        z-index: 1000;
        margin-top: 10px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
        padding: 10px 0;
    }

    .has-dropdown:hover .dropdown-menu {
        opacity: 1;
        visibility: visible;
        margin-top: 0;
    }

    .dropdown-menu ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .dropdown-menu ul li {
        border-bottom: 1px solid rgba(0, 0, 0, 0.05);
    }

    .dropdown-menu ul li:last-child {
        border-bottom: none;
    }

    .dropdown-menu ul li a {
        color: #1a1a1a;
        text-decoration: none;
        padding: 10px 20px;
        display: flex;
        align-items: center;
        font-size: 14px;
        font-weight: 400;
        transition: all 0.3s ease;
        text-transform: uppercase;
    }

    .dropdown-menu ul li a i {
        margin-right: 10px;
        color: #FBBF24;
        font-size: 12px;
    }

    .dropdown-menu ul li a:hover {
        background: rgba(220, 38, 38, 0.05);
        color: #DC2626;
    }

    /* Responsive Design */
    @media (max-width: 1024px) {
        .university-logo-full {
            height: 70px;
        }

        .nav-menu {
            gap: 3px;
        }

        .nav-menu a {
            padding: 8px 12px;
            font-size: 13px;
        }

        .top-bar-content {
            padding: 0 30px;
        }

        .header-content {
            padding: 15px 30px;
        }
    }

    @media (max-width: 768px) {
        /* Top Bar - Mobile */
        .top-bar {
            display: none;
        }

        /* Header - Mobile */
        .header-content {
            padding: 15px 20px;
        }

        .university-logo-full {
            height: 60px;
        }

        /* Show Mobile Menu Toggle */
        .mobile-menu-toggle {
            display: flex;
        }

        /* Mobile Navigation */
        .nav-container {
            position: fixed;
            top: 0;
            right: -100%;
            width: 280px;
            height: 100vh;
            background: white;
            box-shadow: -5px 0 20px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            overflow-y: auto;
            padding-top: 80px;
        }

        .nav-container.active {
            right: 0;
        }

        .nav-menu {
            flex-direction: column;
            gap: 0;
            width: 100%;
        }

        .nav-menu li {
            width: 100%;
            border-bottom: 1px solid #f0f0f0;
        }

        .nav-menu a {
            width: 100%;
            padding: 16px 20px;
            font-size: 14px;
            justify-content: space-between;
        }

        /* Mobile Dropdown */
        .dropdown-menu {
            position: static;
            opacity: 1;
            visibility: visible;
            background: #f8f8f8;
            border: none;
            box-shadow: none;
            margin-top: 0;
            padding: 0;
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease;
        }

        .has-dropdown.active .dropdown-menu {
            max-height: 500px;
        }

        .dropdown-menu ul li a {
            padding: 12px 20px 12px 40px;
            font-size: 13px;
        }

        /* Mobile Dropdown Toggle */
        .has-dropdown > a {
            position: relative;
        }

        .has-dropdown > a .dropdown-icon {
            transition: transform 0.3s ease;
        }

        .has-dropdown.active > a .dropdown-icon {
            transform: rotate(180deg);
        }

        .side-indicator {
            display: none;
        }

        /* Mobile Overlay */
        body.menu-open::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 999;
        }
    }

    @media (max-width: 480px) {
        .university-logo-full {
            height: 50px;
        }

        .nav-container {
            width: 100%;
            right: -100%;
        }
    }
</style>

<script>
    // Mobile Menu Toggle
    document.addEventListener('DOMContentLoaded', function() {
        const mobileMenuToggle = document.getElementById('mobileMenuToggle');
        const navContainer = document.getElementById('navContainer');
        const hasDropdownLinks = document.querySelectorAll('.has-dropdown > a');

        // Toggle mobile menu
        mobileMenuToggle.addEventListener('click', function() {
            this.classList.toggle('active');
            navContainer.classList.toggle('active');
            document.body.classList.toggle('menu-open');
        });

        // Mobile dropdown toggle
        hasDropdownLinks.forEach(link => {
            link.addEventListener('click', function(e) {
                if (window.innerWidth <= 768) {
                    e.preventDefault();
                    const parentLi = this.parentElement;
                    
                    // Close other dropdowns
                    document.querySelectorAll('.has-dropdown').forEach(item => {
                        if (item !== parentLi) {
                            item.classList.remove('active');
                        }
                    });
                    
                    // Toggle current dropdown
                    parentLi.classList.toggle('active');
                }
            });
        });

        // Close menu when clicking outside
        document.addEventListener('click', function(e) {
            if (window.innerWidth <= 768) {
                if (!e.target.closest('.header-content')) {
                    mobileMenuToggle.classList.remove('active');
                    navContainer.classList.remove('active');
                    document.body.classList.remove('menu-open');
                }
            }
        });

        // Close menu on window resize
        window.addEventListener('resize', function() {
            if (window.innerWidth > 768) {
                mobileMenuToggle.classList.remove('active');
                navContainer.classList.remove('active');
                document.body.classList.remove('menu-open');
                document.querySelectorAll('.has-dropdown').forEach(item => {
                    item.classList.remove('active');
                });
            }
        });
    });
</script>
