<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #DC2626;
            --primary-dark: #B91C1C;
            --secondary: #4B5563;
            --success: #10B981;
            --warning: #F59E0B;
            --danger: #EF4444;
            --light: #F9FAFB;
            --dark: #1F2937;
            --border: #E5E7EB;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: var(--light);
            color: var(--dark);
            line-height: 1.6;
        }

        .admin-container {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar - Same as before */
        .sidebar {
            width: 250px;
            background: white;
            border-right: 1px solid var(--border);
            position: fixed;
            height: 100vh;
            overflow-y: auto;
            z-index: 1000;
        }

        .sidebar-header {
            padding: 25px 20px;
            background: var(--primary);
            color: white;
            text-align: center;
        }

        .sidebar-header h2 {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 5px;
        }

        .sidebar-header p {
            font-size: 12px;
            opacity: 0.9;
        }

        .admin-info {
            padding: 20px;
            text-align: center;
            border-bottom: 1px solid var(--border);
        }

        .admin-avatar {
            width: 70px;
            height: 70px;
            background: var(--primary);
            border-radius: 50%;
            margin: 0 auto 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
            font-weight: 600;
        }

        .admin-name {
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 5px;
        }

        .admin-email {
            font-size: 12px;
            color: var(--secondary);
            margin-bottom: 10px;
        }

        .admin-role {
            display: inline-block;
            padding: 4px 12px;
            background: var(--light);
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            color: var(--primary);
        }

        .nav-menu {
            padding: 20px 0;
        }

        .nav-item {
            display: flex;
            align-items: center;
            padding: 12px 20px;
            color: var(--secondary);
            text-decoration: none;
            transition: all 0.3s;
            border-left: 3px solid transparent;
        }

        .nav-item:hover {
            background: var(--light);
            color: var(--primary);
            border-left-color: var(--primary);
        }

        .nav-item.active {
            background: var(--light);
            color: var(--primary);
            border-left-color: var(--primary);
            font-weight: 600;
        }

        .nav-icon {
            width: 24px;
            margin-right: 12px;
            text-align: center;
        }

        .nav-text {
            font-size: 14px;
        }

        .logout-item {
            margin-top: 20px;
            border-top: 1px solid var(--border);
            padding-top: 20px;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: 250px;
            padding: 20px;
        }

        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            margin-bottom: 30px;
            border-bottom: 1px solid var(--border);
        }

        .top-bar h1 {
            font-size: 24px;
            font-weight: 600;
            color: var(--dark);
        }

        .top-bar-actions {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        .back-btn {
            padding: 8px 16px;
            background: var(--secondary);
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 8px;
            text-decoration: none;
        }

        .back-btn:hover {
            background: #374151;
        }

        .logout-btn {
            padding: 8px 16px;
            background: var(--danger);
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .logout-btn:hover {
            background: #DC2626;
        }

        /* Applicant Info */
        .applicant-header {
            background: white;
            border-radius: 10px;
            padding: 25px;
            margin-bottom: 30px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            border: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .applicant-id {
            font-size: 28px;
            font-weight: 700;
            color: var(--primary);
            font-family: monospace;
        }

        .applicant-status {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        .status-badge {
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
        }

        .status-submitted {
            background: rgba(59, 130, 246, 0.1);
            color: #3B82F6;
        }

        .status-under_review {
            background: rgba(245, 158, 11, 0.1);
            color: #F59E0B;
        }

        .status-accepted {
            background: rgba(16, 185, 129, 0.1);
            color: #10B981;
        }

        .status-rejected {
            background: rgba(239, 68, 68, 0.1);
            color: #EF4444;
        }

        .status-on_hold {
            background: rgba(107, 114, 128, 0.1);
            color: #6B7280;
        }

        .payment-badge {
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
        }

        .payment-pending {
            background: rgba(245, 158, 11, 0.1);
            color: #F59E0B;
        }

        .payment-paid {
            background: rgba(16, 185, 129, 0.1);
            color: #10B981;
        }

        .payment-failed {
            background: rgba(239, 68, 68, 0.1);
            color: #EF4444;
        }

        .payment-under_verification {
            background: rgba(59, 130, 246, 0.1);
            color: #3B82F6;
        }

        /* Sections */
        .sections-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .section {
            background: white;
            border-radius: 10px;
            padding: 25px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            border: 1px solid var(--border);
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 1px solid var(--border);
        }

        .section-title {
            font-size: 18px;
            font-weight: 600;
            color: var(--dark);
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
        }

        .info-item {
            margin-bottom: 15px;
        }

        .info-label {
            font-size: 12px;
            font-weight: 600;
            color: var(--secondary);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 5px;
        }

        .info-value {
            font-size: 16px;
            font-weight: 500;
            color: var(--dark);
        }

        /* Documents */
        .documents-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
        }

        .document-card {
            border: 2px dashed var(--border);
            border-radius: 10px;
            padding: 20px;
            text-align: center;
            transition: all 0.3s;
        }

        .document-card:hover {
            border-color: var(--primary);
        }

        .document-icon {
            width: 60px;
            height: 60px;
            background: var(--light);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 15px;
            color: var(--primary);
            font-size: 24px;
        }

        .document-title {
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 10px;
        }

        .document-link {
            display: inline-block;
            padding: 8px 16px;
            background: var(--primary);
            color: white;
            text-decoration: none;
            border-radius: 6px;
            font-size: 12px;
            font-weight: 600;
            transition: all 0.3s;
        }

        .document-link:hover {
            background: var(--primary-dark);
        }

        /* Admin Actions */
        .action-section {
            background: white;
            border-radius: 10px;
            padding: 25px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            border: 1px solid var(--border);
        }

        .action-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            font-size: 14px;
            font-weight: 600;
            color: var(--secondary);
            margin-bottom: 8px;
        }

        .form-control {
            width: 100%;
            padding: 10px 12px;
            border: 2px solid var(--border);
            border-radius: 6px;
            font-size: 14px;
            transition: all 0.3s;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary);
        }

        textarea.form-control {
            min-height: 100px;
            resize: vertical;
        }

        .action-btn {
            padding: 12px 24px;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .action-btn:hover {
            background: var(--primary-dark);
        }

        .btn-success {
            background: var(--success);
        }

        .btn-success:hover {
            background: #0D9C6E;
        }

        .btn-warning {
            background: var(--warning);
        }

        .btn-warning:hover {
            background: #D97706;
        }

        .btn-danger {
            background: var(--danger);
        }

        .btn-danger:hover {
            background: #DC2626;
        }

        /* Notes */
        .notes-section {
            background: white;
            border-radius: 10px;
            padding: 25px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            border: 1px solid var(--border);
            margin-top: 30px;
        }

        .notes-list {
            margin-top: 20px;
        }

        .note-item {
            padding: 15px;
            border: 1px solid var(--border);
            border-radius: 8px;
            margin-bottom: 15px;
            background: var(--light);
        }

        .note-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .note-admin {
            font-weight: 600;
            color: var(--primary);
        }

        .note-date {
            font-size: 12px;
            color: var(--secondary);
        }

        .note-content {
            font-size: 14px;
            color: var(--dark);
        }

        /* Mobile menu button */
        .mobile-menu-btn {
            display: none;
            position: fixed;
            top: 20px;
            left: 20px;
            z-index: 1001;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 6px;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            cursor: pointer;
        }

        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s;
            }

            .sidebar.active {
                transform: translateX(0);
            }

            .main-content {
                margin-left: 0;
            }

            .mobile-menu-btn {
                display: flex;
            }

            .sections-grid {
                grid-template-columns: 1fr;
            }

            .applicant-header {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <button class="mobile-menu-btn" onclick="toggleSidebar()">
        <i class="fas fa-bars"></i>
    </button>

    <div class="admin-container">
        <!-- Sidebar -->
        <div class="sidebar" id="sidebar">
            <div class="sidebar-header">
                <h2><i class="fas fa-university"></i> Admin Panel</h2>
                <p>Swaminarayan University</p>
            </div>

            <div class="admin-info">
                <div class="admin-avatar">
                    <%= admin.name.charAt(0).toUpperCase() %>
                </div>
                <div class="admin-name"><%= admin.name %></div>
                <div class="admin-email"><%= admin.email %></div>
                <div class="admin-role"><%= admin.role %></div>
            </div>

            <div class="nav-menu">
                <a href="/admin/dashboard" class="nav-item">
                    <div class="nav-icon">
                        <i class="fas fa-tachometer-alt"></i>
                    </div>
                    <div class="nav-text">Dashboard</div>
                </a>

                <a href="/admin/applicants" class="nav-item">
                    <div class="nav-icon">
                        <i class="fas fa-users"></i>
                    </div>
                    <div class="nav-text">All Applicants</div>
                </a>

                <a href="/admin/export" class="nav-item">
                    <div class="nav-icon">
                        <i class="fas fa-download"></i>
                    </div>
                    <div class="nav-text">Export Data</div>
                </a>

                <a href="/admin/settings" class="nav-item">
                    <div class="nav-icon">
                        <i class="fas fa-cog"></i>
                    </div>
                    <div class="nav-text">Settings</div>
                </a>

                <div class="logout-item">
                    <a href="/admin/logout" class="nav-item">
                        <div class="nav-icon">
                            <i class="fas fa-sign-out-alt"></i>
                        </div>
                        <div class="nav-text">Logout</div>
                    </a>
                </div>
            </div>
        </div>

        <!-- Main Content -->
        <div class="main-content">
            <div class="top-bar">
                <h1>
                    <i class="fas fa-user-graduate"></i> Applicant Details
                </h1>
                <div class="top-bar-actions">
                    <a href="/admin/applicants" class="back-btn">
                        <i class="fas fa-arrow-left"></i> Back to List
                    </a>
                    <button class="logout-btn" onclick="logout()">
                        <i class="fas fa-sign-out-alt"></i> Logout
                    </button>
                </div>
            </div>

            <!-- Applicant Header -->
            <div class="applicant-header">
                <div>
                    <div class="applicant-id"><%= applicant.applicationId %></div>
                    <div style="font-size: 20px; font-weight: 600; margin-top: 10px;">
                        <%= applicant.fullName %>
                    </div>
                </div>
                <div class="applicant-status">
                    <span class="payment-badge payment-<%= applicant.paymentStatus %>">
                        Payment: <%= applicant.paymentStatus.replace('_', ' ') %>
                    </span>
                    <span class="status-badge status-<%= applicant.status %>">
                        Status: <%= applicant.status.replace('_', ' ') %>
                    </span>
                </div>
            </div>

            <!-- Personal & Academic Info -->
            <div class="sections-grid">
                <!-- Personal Details -->
                <div class="section">
                    <div class="section-header">
                        <div class="section-title">
                            <i class="fas fa-user"></i> Personal Details
                        </div>
                    </div>
                    <div class="info-grid">
                        <div class="info-item">
                            <div class="info-label">Full Name</div>
                            <div class="info-value"><%= applicant.fullName %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Email</div>
                            <div class="info-value"><%= applicant.email %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Phone</div>
                            <div class="info-value"><%= applicant.phone %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Date of Birth</div>
                            <div class="info-value">
                                <%= new Date(applicant.dob).toLocaleDateString() %>
                            </div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Gender</div>
                            <div class="info-value" style="text-transform: capitalize;">
                                <%= applicant.gender %>
                            </div>
                        </div>
                        <div class="info-item" style="grid-column: 1 / -1;">
                            <div class="info-label">Address</div>
                            <div class="info-value"><%= applicant.address %></div>
                        </div>
                    </div>
                </div>

                <!-- Academic Details -->
                <div class="section">
                    <div class="section-header">
                        <div class="section-title">
                            <i class="fas fa-graduation-cap"></i> Academic Details
                        </div>
                    </div>
                    <div class="info-grid">
                        <div class="info-item">
                            <div class="info-label">Qualification</div>
                            <div class="info-value"><%= applicant.qualification %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Board/University</div>
                            <div class="info-value"><%= applicant.boardUniversity %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Year of Passing</div>
                            <div class="info-value"><%= applicant.passingYear %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Percentage/CGPA</div>
                            <div class="info-value"><%= applicant.percentage %>%</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Program & Documents -->
            <div class="sections-grid">
                <!-- Program Details -->
                <div class="section">
                    <div class="section-header">
                        <div class="section-title">
                            <i class="fas fa-book"></i> Program Details
                        </div>
                    </div>
                    <div class="info-grid">
                        <div class="info-item">
                            <div class="info-label">Program Type</div>
                            <div class="info-value" style="text-transform: capitalize;">
                                <%= applicant.programType %>
                            </div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Course Name</div>
                            <div class="info-value"><%= applicant.courseName %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Application Fee</div>
                            <div class="info-value">₹<%= applicant.applicationFee %></div>
                        </div>
                        <div class="info-item">
                            <div class="info-label">Applied Date</div>
                            <div class="info-value">
                                <%= new Date(applicant.createdAt).toLocaleDateString() %>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Documents -->
<!-- Documents -->
<div class="section">
  <div class="section-header">
    <div class="section-title">
      <i class="fas fa-file"></i> Documents
    </div>
  </div>

  <div class="documents-grid">

    <!-- Passport Photo -->
    <div class="document-card">
      <div class="document-icon">
        <i class="fas fa-camera"></i>
      </div>
      <div class="document-title">Passport Photo</div>

      <% if (applicant.photoUrl) { %>
        <a
          href="/<%= applicant.photoUrl.replace(/^\/+/, '') %>"
          target="_blank"
          class="document-link"
        >
          <i class="fas fa-download"></i> View
        </a>
      <% } else { %>
        <span style="color: var(--danger); font-size: 12px;">Not uploaded</span>
      <% } %>
    </div>

    <!-- ID Proof -->
    <div class="document-card">
      <div class="document-icon">
        <i class="fas fa-id-card"></i>
      </div>
      <div class="document-title">ID Proof</div>

      <% if (applicant.idProofUrl) { %>
        <a
          href="/<%= applicant.idProofUrl.replace(/^\/+/, '') %>"
          target="_blank"
          class="document-link"
        >
          <i class="fas fa-download"></i> View
        </a>
      <% } else { %>
        <span style="color: var(--danger); font-size: 12px;">Not uploaded</span>
      <% } %>
    </div>

    <!-- Academic Certificate -->
    <div class="document-card">
      <div class="document-icon">
        <i class="fas fa-certificate"></i>
      </div>
      <div class="document-title">Academic Certificate</div>

      <% if (applicant.certificateUrl) { %>
        <a
          href="/<%= applicant.certificateUrl.replace(/^\/+/, '') %>"
          target="_blank"
          class="document-link"
        >
          <i class="fas fa-download"></i> View
        </a>
      <% } else { %>
        <span style="color: var(--danger); font-size: 12px;">Not uploaded</span>
      <% } %>
    </div>

  </div>
</div>
            </div>

            <!-- Payment Details -->
            <% if (applicant.paymentMethod) { %>
                <div class="section">
                    <div class="section-header">
                        <div class="section-title">
                            <i class="fas fa-credit-card"></i> Payment Details
                        </div>
                    </div>
                    <div class="info-grid">
                        <div class="info-item">
                            <div class="info-label">Payment Method</div>
                            <div class="info-value" style="text-transform: uppercase;">
                                <%= applicant.paymentMethod %>
                            </div>
                        </div>
                        <% if (applicant.paymentMethod === 'utr') { %>
                            <div class="info-item">
                                <div class="info-label">UTR Number</div>
                                <div class="info-value"><%= applicant.utrNumber %></div>
                            </div>
                            <div class="info-item">
                                <div class="info-label">Payment Date</div>
                                <div class="info-value">
                                    <%= new Date(applicant.paymentDate).toLocaleDateString() %>
                                </div>
                            </div>
                            <div class="info-item">
                                <div class="info-label">Payment Mode</div>
                                <div class="info-value"><%= applicant.paymentMode %></div>
                            </div>
                        <% } else if (applicant.paymentMethod === 'gateway') { %>
                            <div class="info-item">
                                <div class="info-label">Razorpay Order ID</div>
                                <div class="info-value"><%= applicant.razorpayOrderId %></div>
                            </div>
                        <% } %>
                        <% if (applicant.paymentScreenshotUrl) { %>
                            <div class="info-item">
                                <div class="info-label">Payment Proof</div>
                                <a href="<%= applicant.paymentScreenshotUrl %>" 
                                   target="_blank" 
                                   class="document-link" 
                                   style="padding: 8px 16px; margin-top: 5px; display: inline-block;">
                                    <i class="fas fa-download"></i> View Receipt
                                </a>
                            </div>
                        <% } %>
                    </div>
                </div>
            <% } %>

            <!-- Admin Actions -->
            <div class="action-section">
                <div class="section-header">
                    <div class="section-title">
                        <i class="fas fa-cogs"></i> Update Status
                    </div>
                </div>
                
                <div class="action-grid">
                    <!-- Update Application Status -->
                    <form action="/admin/applicant/<%= applicant._id %>/status" method="POST">
                        <div class="form-group">
                            <label for="status">Application Status</label>
                            <select name="status" id="status" class="form-control" required>
                                <option value="submitted" <%= applicant.status === 'submitted' ? 'selected' : '' %>>Submitted</option>
                                <option value="under_review" <%= applicant.status === 'under_review' ? 'selected' : '' %>>Under Review</option>
                                <option value="accepted" <%= applicant.status === 'accepted' ? 'selected' : '' %>>Accepted</option>
                                <option value="rejected" <%= applicant.status === 'rejected' ? 'selected' : '' %>>Rejected</option>
                                <option value="on_hold" <%= applicant.status === 'on_hold' ? 'selected' : '' %>>On Hold</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="adminNote">Add Note (Optional)</label>
                            <textarea name="adminNote" id="adminNote" class="form-control" 
                                      placeholder="Add a note about this status change"></textarea>
                        </div>
                        <button type="submit" class="action-btn">
                            <i class="fas fa-save"></i> Update Status
                        </button>
                    </form>

                    <!-- Update Payment Status -->
                    <form action="/admin/applicant/<%= applicant._id %>/payment-status" method="POST">
                        <div class="form-group">
                            <label for="paymentStatus">Payment Status</label>
                            <select name="paymentStatus" id="paymentStatus" class="form-control" required>
                                <option value="pending" <%= applicant.paymentStatus === 'pending' ? 'selected' : '' %>>Pending</option>
                                <option value="paid" <%= applicant.paymentStatus === 'paid' ? 'selected' : '' %>>Paid</option>
                                <option value="failed" <%= applicant.paymentStatus === 'failed' ? 'selected' : '' %>>Failed</option>
                                <option value="under_verification" <%= applicant.paymentStatus === 'under_verification' ? 'selected' : '' %>>Under Verification</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="paymentNote">Add Note (Optional)</label>
                            <textarea name="adminNote" id="paymentNote" class="form-control" 
                                      placeholder="Add a note about payment verification"></textarea>
                        </div>
                        <button type="submit" class="action-btn btn-success">
                            <i class="fas fa-money-bill-wave"></i> Update Payment
                        </button>
                    </form>
                </div>
            </div>

            <!-- Admin Notes -->
            <% if (applicant.adminNotes && applicant.adminNotes.length > 0) { %>
                <div class="notes-section">
                    <div class="section-header">
                        <div class="section-title">
                            <i class="fas fa-sticky-note"></i> Admin Notes
                        </div>
                    </div>
                    <div class="notes-list">
                        <% applicant.adminNotes.forEach(note => { %>
                            <div class="note-item">
                                <div class="note-header">
                                    <div class="note-admin">
                                        <i class="fas fa-user-shield"></i> <%= note.adminName %>
                                    </div>
                                    <div class="note-date">
                                        <%= new Date(note.date).toLocaleDateString() %>
                                    </div>
                                </div>
                                <div class="note-content">
                                    <%= note.note %>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('active');
        }

        function logout() {
            window.location.href = '/admin/logout';
        }

        // Close sidebar when clicking outside on mobile
        document.addEventListener('click', function(event) {
            const sidebar = document.getElementById('sidebar');
            const mobileBtn = document.querySelector('.mobile-menu-btn');
            
            if (window.innerWidth <= 768 && 
                !sidebar.contains(event.target) && 
                !mobileBtn.contains(event.target)) {
                sidebar.classList.remove('active');
            }
        });
    </script>
</body>
</html>