<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&family=Montserrat:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
        }

        body { 
            font-family: 'Roboto', sans-serif;
            color: #1a1a1a;
            line-height: 1.6;
            min-height: 100vh;
            position: relative;
            overflow-x: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 800;
            letter-spacing: -0.5px;
        }

        /* Full page background image */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://images.unsplash.com/photo-1523050854058-8df90110c9f1?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            z-index: -2;
        }

        body::after {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(248, 248, 248, 0.95);
            z-index: -1;
        }

        .auth-container {
            width: 100%;
            max-width: 1000px;
            background: #ffffff;
            border: 2px solid #e5e5e5;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            padding: 50px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
            max-height: 85vh;
            overflow-y: auto;
        }

        /* Left Side: University Branding */
        .university-branding {
            padding-right: 30px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .section-title {
            font-size: 36px;
            font-weight: 800;
            color: #1a1a1a;
            margin-bottom: 40px;
            letter-spacing: -1px;
            text-transform: uppercase;
            position: relative;
            padding-bottom: 15px;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 50px;
            height: 3px;
            background: #DC2626;
        }

        .section-title .highlight {
            color: #DC2626;
        }

        .university-subtitle {
            font-size: 18px;
            color: #666;
            max-width: 450px;
            line-height: 1.7;
            font-weight: 300;
            letter-spacing: 0.2px;
            margin-bottom: 30px;
        }

        .university-features {
            list-style: none;
            flex-grow: 1;
        }

        .university-features li {
            font-size: 15px;
            color: #1a1a1a;
            padding: 15px 0 15px 0;
            position: relative;
            border-bottom: 1px solid #f0f0f0;
            display: flex;
            align-items: flex-start;
            gap: 12px;
        }

        .university-features li:last-child {
            border-bottom: none;
        }

        .university-features li i {
            color: #DC2626;
            font-size: 16px;
            margin-top: 2px;
            flex-shrink: 0;
        }

        /* Right Side: Admin Login Form */
        .form-section {
            padding-left: 30px;
            display: flex;
            flex-direction: column;
        }

        /* Error Message */
        .error-container {
            min-height: 60px;
            margin-bottom: 10px;
        }

        .error { 
            color: #DC2626; 
            font-size: 13px; 
            padding: 14px 16px;
            background: #FEF2F2;
            border: 1px solid #FECACA;
            border-left: 4px solid #DC2626;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 15px;
            animation: slideDown 0.3s ease;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .admin-badge {
            background: linear-gradient(135deg, #DC2626, #B91C1C);
            color: white;
            padding: 12px 24px;
            border-radius: 50px;
            font-size: 14px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 30px;
            text-align: center;
            font-family: 'Montserrat', sans-serif;
            box-shadow: 0 5px 15px rgba(220, 38, 38, 0.3);
        }

        .form-group {
            margin-bottom: 18px;
        }

        label { 
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 12px; 
            font-weight: 700; 
            margin-bottom: 8px; 
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: #1a1a1a;
            transition: color 0.3s ease;
        }

        label i {
            color: #DC2626;
            width: 16px;
            font-size: 13px;
        }

        input { 
            width: 100%; 
            padding: 14px 16px; 
            border: 2px solid #e5e5e5; 
            font-size: 15px;
            font-family: 'Roboto', sans-serif;
            transition: all 0.3s ease;
            background: #ffffff;
            border-radius: 8px;
        }

        input:focus { 
            border-color: #DC2626; 
            outline: none;
            box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.1);
            transform: translateY(-1px);
        }

        .btn { 
            width: 100%; 
            padding: 18px 24px; 
            background: linear-gradient(135deg, #DC2626 0%, #B91C1C 100%);
            color: #fff; 
            border: 2px solid #DC2626; 
            text-transform: uppercase; 
            font-weight: 700; 
            font-size: 14px;
            cursor: pointer;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            font-family: 'Montserrat', sans-serif;
            margin-top: 10px;
            border-radius: 8px;
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn:hover { 
            background: linear-gradient(135deg, #B91C1C 0%, #991B1B 100%);
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(220, 38, 38, 0.25);
            border-color: #B91C1C;
        }

        .login-footer {
            margin-top: auto;
            text-align: center;
            padding-top: 30px;
            border-top: 2px solid #e5e5e5;
            font-size: 13px;
            color: #666;
            font-weight: 500;
        }

        /* Responsive */
        @media (max-width: 992px) {
            .auth-container {
                grid-template-columns: 1fr;
                gap: 40px;
                padding: 40px 30px;
                max-height: none;
            }

            .form-section {
                padding-left: 0;
            }

            .university-branding {
                padding-right: 0;
            }
        }

        @media (max-width: 768px) {
            body {
                padding: 15px;
            }
            
            .auth-container {
                padding: 30px 25px;
                gap: 30px;
            }
            
            .section-title {
                font-size: 28px;
                margin-bottom: 30px;
            }
        }

        @media (max-width: 480px) {
            .auth-container {
                padding: 25px 20px;
                gap: 25px;
            }
            
            .section-title {
                font-size: 24px;
            }

            input {
                padding: 12px 14px;
            }

            .btn {
                padding: 16px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <!-- Left Side: University Branding -->
        <div class="university-branding">
            <h2 class="section-title">Admin <span class="highlight">Portal</span></h2>
            <p class="university-subtitle">
                Secure access to Swaminarayan University Application Management System. 
                Monitor admissions, verify applications, and manage student records.
            </p>
            
            <ul class="university-features">
                <li><i class="fas fa-shield-alt"></i>Secure Admin Dashboard</li>
                <li><i class="fas fa-users-cog"></i>Application Management</li>
                <li><i class="fas fa-chart-line"></i>Real-time Analytics</li>
                <li><i class="fas fa-file-invoice-dollar"></i>Payment Verification</li>
                <li><i class="fas fa-user-check"></i>Student Verification</li>
            </ul>
        </div>

        <!-- Right Side: Admin Login Form -->
        <div class="form-section">
            <!-- Admin Badge -->
            <div class="admin-badge">
                <i class="fas fa-user-shield"></i> Authorized Personnel Only
            </div>

            <!-- Error Container -->
            <div class="error-container" id="errorContainer">
                <% if (error) { %>
                    <div class="error">
                        <i class="fas fa-exclamation-triangle"></i>
                        <%= error %>
                    </div>
                <% } %>
            </div>

            <form action="/admin/login" method="POST">
                <div class="form-group">
                    <label for="username">
                        <i class="fas fa-user"></i> Username or Email
                    </label>
                    <input type="text" 
                           id="username" 
                           name="username" 
                           placeholder="Enter username or email"
                           required
                           autofocus>
                </div>
                
                <div class="form-group">
                    <label for="password">
                        <i class="fas fa-lock"></i> Password
                    </label>
                    <input type="password" 
                           id="password" 
                           name="password" 
                           placeholder="Enter your password"
                           required>
                </div>
                
                <button type="submit" class="btn">
                    <i class="fas fa-sign-in-alt"></i> Admin Login
                </button>
            </form>

            <div class="login-footer">
                <p><i class="fas fa-info-circle"></i> For security reasons, please log out when finished</p>
            </div>
        </div>
    </div>

    <script>
        // Simple form validation
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.querySelector('form');
            const username = document.getElementById('username');
            const password = document.getElementById('password');

            form.addEventListener('submit', function(e) {
                if (!username.value.trim() || !password.value.trim()) {
                    e.preventDefault();
                    showError('Please fill all fields');
                    return false;
                }
            });

            function showError(message) {
                const errorContainer = document.getElementById('errorContainer');
                let existingError = errorContainer.querySelector('.error');
                
                if (existingError) {
                    existingError.remove();
                }
                
                const errorDiv = document.createElement('div');
                errorDiv.className = 'error';
                errorDiv.innerHTML = `
                    <i class="fas fa-exclamation-triangle"></i>
                    ${message}
                `;
                errorContainer.appendChild(errorDiv);
            }
        });
    </script>
</body>
</html>
