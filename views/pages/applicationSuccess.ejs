<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: #f8f8f8;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .success-container {
            background: white;
            border: 2px solid #e5e5e5;
            padding: 60px 40px;
            max-width: 600px;
            text-align: center;
        }

        .success-icon {
            width: 100px;
            height: 100px;
            background: #10B981;
            margin: 0 auto 30px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .success-icon i {
            font-size: 50px;
            color: white;
        }

        h1 {
            font-size: 32px;
            color: #1a1a1a;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: -1px;
        }

        p {
            font-size: 16px;
            color: #666;
            line-height: 1.6;
            margin-bottom: 30px;
        }

        .application-id {
            background: #f8f8f8;
            border: 2px solid #e5e5e5;
            padding: 20px;
            margin-bottom: 30px;
        }

        .application-id strong {
            font-size: 18px;
            color: #DC2626;
        }

        .btn {
            display: inline-block;
            padding: 16px 40px;
            background: #DC2626;
            color: white;
            text-decoration: none;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            border: 2px solid #DC2626;
        }

        .btn:hover {
            background: transparent;
            color: #DC2626;
        }
    </style>
</head>
<body>
    <div class="success-container">
        <div class="success-icon">
            <i class="fas fa-check"></i>
        </div>
        <h1>Application Submitted Successfully!</h1>
        <p>Your application has been received and payment has been confirmed. You will receive a confirmation email shortly.</p>
        
        <div class="application-id">
            <p>Your Application ID:</p>
            <strong>SU2025-<%= applicationId %></strong>
        </div>

        <p>Please save this application ID for future reference.</p>

        <a href="/" class="btn">
            <i class="fas fa-home"></i> Back to Home
        </a>
    </div>
</body>
</html>
